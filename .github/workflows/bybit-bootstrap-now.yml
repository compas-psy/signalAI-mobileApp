name: One-shot Bybit cold bootstrap now

# Disabled after the bounded production bootstrap was started. Keeping the file
# only for auditability; no further push on the diagnostic branch can launch a
# second writer while the original run is active.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This one-shot bootstrap is intentionally disabled after launch.'
