# SignalAI v3 — что реализовано и что осталось

Отчёт по ТЗ `SignalAI-Design-Spec-v3.md` + `AGENT_IMPLEMENTATION_SPEC.md`.
Формат по §23: что сделано, чем проверить, какие ограничения остались.

---

## Реализовано

### Фаза 1 — книга капитала (фундамент)

| Что | Где | Проверка |
|---|---|---|
| Деньги в фиксированной точке (`Money`, `Quantity`, `Currency`) | `domain/ledger/money.dart` | `test/capital_ledger_test.dart` |
| Неизменяемые события книги, 15 типов операций | `domain/ledger/ledger_event.dart` | там же |
| Хранилище только на дозапись, версия схемы, дедуп по id | `data/ledger/ledger_store.dart` | там же |
| Детерминированные проекции: остатки, лоты FIFO, позиции, P&L | `domain/ledger/projections.dart` | там же |
| Разбор выписки Т-Инвестиций в события | `data/ledger/broker_import.dart` | там же |

**31 тест финансовой корректности**: частичные продажи, FIFO по нескольким
лотам, шорт и разворот одной сделкой, фандинг в обе стороны, вармаржа,
валюта без курса, порядок событий, корректировка без стирания истории,
повторный импорт выписки.

Ключевые решения:

- **`double` для денег запрещён.** `0,1 + 0,2` в двоичной плавающей точке даёт
  `0,30000000000000004`; на тысяче операций баланс перестаёт сходиться.
- **Разные валюты не складываются** — падает с ошибкой, а не превращается в
  «105 непонятно чего».
- **Порядок применения — по времени операции**, а не по порядку записи в файл:
  запоздавший импорт встаёт на своё место и не портит историю.
- **Снимок брокера не переписывает книгу**, а создаёт наблюдение сверки: иначе
  сбой площадки молча стирал бы историю владельца.
- **Неизвестный тип операции не подгоняется под похожий**: попадает в книгу
  корректировкой с исходным описанием. Молчаливое «всё остальное — комиссия»
  однажды превратило бы дивиденд в издержку.

### Фаза 2 — каркас v3

- Пять разделов: **Сегодня · Капитал · Торговля · Лаборатория · Контроль**;
  подразделы — пилюли в шапке, глубина максимум два уровня.
- Миграция маршрутов версии 2 (`AppRoute.fromLegacy`), тесты в
  `test/navigation_test.dart`.
- Шапка раздела: заголовок, время данных, чип режима риск-движка
  (NORMAL / CAUTION / REDUCE ONLY / KILL SWITCH) — индикатор, а не переключатель.
- Аварийная остановка отдельной красной кнопкой в рейле планшета.
- Токены v3: `border #24242B`, `textMuted #9696A1`, `textFaint #666670`,
  `info #63A5FF`, `tactical #B38CFF`, `warning #FFB74D`. Семантика строгая:
  жёлтый — только действие.

### Экран «Сегодня»

Всё из настоящих чисел книги: совокупный капитал, внесено и результат,
полоса аллокации по контурам факт против цели, свободный кэш, стоимость
позиций, нереализованный и чистый результат, разложение «из чего результат»
расходящимися барами, здоровье данных, кнопка сверки.

**Очередь решений** собирается из фактического состояния: неподтверждённые
идеи, расхождения книги, книга без диска, позиции без стопа (первыми — это
дефект). Пустая очередь объясняет, почему она пуста.

### Раздел «Капитал»

- **Обзор** — внесено, стоимость, реализовано, нереализовано; доходы,
  издержки, разрез по валютам и контурам.
- **Счета** — карточка на счёт: тип, валюта, остаток, статус сверки, время
  синхронизации, права ключа. Право на вывод не запрашивается, и это написано.
- **Пакеты** — целевые корзины; ожидаемая доходность не показывается, пока её
  нечем подтвердить.
- **Книга** — фильтры (все · сделки · деньги · доходы · издержки · правки),
  разбор операции до идентификатора, **ручной ввод операции** и заведение
  счёта. Знак задаёт тип: вывод не может быть положительным.
- **Аналитика** — результат, издержки, честная оговорка про TWR/XIRR.

### Торговый цикл

- **Влияние на портфель** в шите подтверждения: каким станет открытый риск в
  процентах капитала, сколько сделок будет одновременно, есть ли позиция той
  же группы корреляции. Подход к лимиту (80%) предупреждает.
- После подтверждения запускается сверка, а не запись «на всякий случай»:
  заявка — ещё не позиция.
- **Импорт выписки** Т-Инвестиций наполняет книгу сам.

---

### Фаза 5 — капитал из интеграций, опционы, пакеты

| Что | Где | Проверка |
|---|---|---|
| Все счета Т-Инвестиций и выбор торгового | `data/broker/tinvest_broker.dart` | `test/tinvest_broker_test.dart` |
| Bybit: кошелёк и журнал операций в книгу | `data/broker/bybit_broker.dart` | `test/capital_ledger_test.dart` |
| Опционы: Блэк-76, греки, волатильность из цены | `domain/options/black76.dart` | `test/options_test.dart` |
| Конструкции с ограниченным риском | `domain/options/structures.dart` | там же |
| Инструкция к исполнению | `domain/options/execution_ticket.dart` | там же |
| Пакеты: полосы, ребалансировка, история | `domain/portfolio/` | `test/packages_test.dart` |

Ключевые решения фазы 5:

- **Токен Invest API видит все счета.** Он привязан к пользователю, а не к
  счёту: `GetAccounts` возвращает и фьючерсный, и тот, где лежит основной
  капитал. Второй токен не нужен — нужен был обход счетов.
- **Торговый счёт выбирается явно**, остальные строго на чтение. Заявка не с
  того счёта ломает и учёт, и налоги.
- **У Bybit своя таблица типов.** Словари площадок разные; строка TRADE — это
  исполнение вместе с комиссией, а не покупка бумаги.
- **Блэк-76, а не Блэк-Шоулз**: базовый актив опциона на FORTS — фьючерс.
- **Голая продажа не предлагается по построению.** Покрытый колл появляется
  только при наличии позиции; проверка ограниченности убытка учитывает, что
  цена не уходит ниже нуля.
- **Порядок ног в инструкции обязателен**: сначала покупки. Если сначала
  продать и вторая нога не исполнится, останется голая продажа.
- **Полоса допуска и порог сделки** — то, чем ребалансировка отличается от
  арифметики: иначе комиссия съедает ровно ту доходность, ради которой всё
  затевалось.
- **Доходность пакета — история, а не прогноз**, на реальных сериях MOEX со
  стресс-срезами 2022 и 2020.

Одну проверку из среды сборки сделать нельзя: доступа к MOEX там нет, а
состав колонок опционной выдачи ISS биржа меняет. Поэтому в «Контроль →
Диагностика данных» добавлен живой прогон цепочки: видно, сколько пришло
контрактов, страйков, цен, волатильности и открытого интереса.

---

### Фаза 6 — починка оборванных путей данных

Владелец установил сборку и получил пять отказов подряд. Ни один из них не
был вопросом оформления: экраны стояли поверх путей данных, которые в
работающем приложении не выполнялись.

| Что не работало | Почему | Что сделано |
|---|---|---|
| На «Сегодня» нет сверки | Подгрузка данных висела только на переходах между разделами, а приложение стартует уже на «Сегодня». Плюс кнопка сверки жила в блоке, который рисовался при непустой книге — наполнить книгу было нечем | Книга читается в `load()`; блок здоровья данных и сверка показываются всегда |
| В счетах только Т-Инвестиции | Режим площадки сбрасывался на тестнет, а ключ ищется по паре «площадка + режим»: Bybit молча исчезал | Загрузка состояния стала общим `Future`, запись дожидается чтения; `brokerKeyModes`/`readableMode` читают площадку тем режимом, для которого есть ключи |
| Нет цепочки опционов | Запрос тянул всю доску MOEX по сто строк без фильтра по активу: до SI обход не доходил, сорок запросов вешали экран | Фильтр по активу, ранний выход, бюджет времени, протокол запроса |
| Диагностика стояла на цепочке | Та же строка, тот же запрос | Проверка ушла в конец списка и печатает протокол |
| Пакет не раскрывался | Карточка не нажималась, разбора не было | Экран деталей: состав до инструментов в лотах, штуках и деньгах |

Ключевые решения фазы 6:

- **Гонка загрузки состояния — причина «всегда тестнет».** Флаг «загружено»
  взводился до первого `await`, а запуск читает журнал, стратегии и
  настройки параллельно: второй и третий вызовы работали с дефолтами и
  возвращали их на диск. Защита теперь стоит в самой записи, а не в каждом
  из полутора десятков вызывающих.
- **Режим и ключи разведены по назначению.** Читать живой счёт ключом live
  при переключателе на testnet безопасно — запросы только читающие. Слать
  туда заявку нельзя, и это правило не смягчено.
- **Площадка перечисляется всегда**, даже без ключей: строка с причиной
  честнее пустоты. Пустота читается как «этой биржи в приложении нет».
- **Пустая цепочка не кэшируется.** Одна неудача запирала раздел на десять
  минут, сколько кнопку ни жми.
- **Экран «Сырой ответ биржи»** — потому что схему выдачи ISS нельзя
  проверить оттуда, где собирается приложение. Один скриншот вместо
  переписки догадками.
- **Пакет исполним, а не описан.** У класса есть конкретный инструмент,
  количество считается в лотах с округлением вниз, остаток называется
  отдельной строкой, инструмент без цены не подменяется похожим.
- **Кривая капитала строится из отметок, а не пересчётом прошлого.** Цен
  вчерашнего дня никто не хранит; пересчёт сегодняшними ценами дал бы
  красивый график, который врёт ровно на движение рынка. Отметка ставится
  при каждой сверке, дельты считаются за вычетом пополнений.

---

## Осталось

| Что | Почему не сделано |
|---|---|
| **Исполнение опционов приложением** | Многоногая заявка исполняется ногами; частичное исполнение оставляет незакрытый риск. Пока приложение выдаёт инструкцию, а заявку ставит владелец |
| **TWR / MWR (XIRR)** | Требуют стоимости капитала на даты потоков. Появятся, когда книга накопит историю за несколько периодов; пока показан простой процент с оговоркой, что он занижает результат при поздних пополнениях |
| **Журнал сделок**: дисциплина плана, MAE/MFE, версии плана | Нужна связка Trade → Order → Fill поверх книги; сейчас журнал бумажных сделок работает по-старому |
| **Кривая капитала во времени** | Проекция на даты уже есть (`project(asOf:)`), нужен обход дат и кэш |
| **AI Copilot** | Отдельная фаза; требует контракта «ответ только со ссылкой на источник» |

---

## Отступление от ТЗ, о котором нужно знать

ТЗ требует SQLite (предпочтительно Drift). Реализовано хранилище
**журналом только на дозапись** (NDJSON с версией схемы), и вот почему:

1. Книга по модели неизменяема — `UPDATE` и `DELETE` в ней быть не должно.
   Файл-журнал выражает это точнее, чем таблица, где они возможны.
2. Миграции сводятся к версии схемы в заголовке, транзакция — к атомарной
   дозаписи; временный файл плюс переименование при полной перезаписи.
3. Не появляется нативной зависимости (`sqlite3`) в приложении, которое
   подтверждает сделки: это ещё одна поверхность отказа на устройстве
   владельца, а проверить её сборку на этом устройстве нечем.

Хранилище спрятано за интерфейсом: если книга вырастет до сотен тысяч
операций и линейный разбор станет заметен, замена на SQLite не затронет
домен и экраны.

---

## Как проверить руками

1. **Сегодня** сразу после запуска: виден блок «Здоровье данных» и кнопка
   «Сверить с площадками» — даже при пустой книге. Нажать: если ключи
   заданы, в книгу импортируется выписка за год, появляются счета и позиции.
   KPI-плитки нажимаются: каждая раскрывает свой расчёт.
2. **Капитал → Книга** → «Записать операцию»: заведите начальный остаток —
   капитал, аллокация и «Из чего результат» пересчитаются сразу.
3. **Капитал → Книга** → тап по строке: разбор до идентификатора, ссылки
   брокера и статуса сверки.
4. **Торговля → Идеи** → идея → «Отправить на биржу»: в шите виден блок
   «Влияние на портфель».
5. **Капитал → Счета**: перечислены все площадки — Т-Инвестиции со своими
   счетами и Bybit. У площадки без ключей или с расхождением режима — строка
   с причиной, а не пустота.
6. **Торговля → Опционы**: цепочка SI, конструкция, диаграмма выплат, греки
   и копируемая инструкция к исполнению. Если цепочки нет — видно, каким
   путём её спрашивали и что ответила биржа, плюс кнопка сырого ответа.
7. **Капитал → Пакеты** → нажать пакет: состав до инструментов (лоты, штуки,
   суммы, «докупить N шт. на M ₽», остаток кэшем), кривая исторической
   симуляции со стресс-срезами, заявки ребалансировки с оборотом.
8. **Контроль → Интеграции**: список счетов Т-Инвестиций, выбор торгового.
9. **Контроль → Безопасность → Диагностика данных**: живой прогон цепочки
   опционов с протоколом запроса и кнопка «Сырой ответ биржи» — какие поля
   реально отдаёт ISS по четырём разным адресам.
