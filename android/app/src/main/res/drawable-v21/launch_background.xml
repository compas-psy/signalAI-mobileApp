<?xml version="1.0" encoding="utf-8"?>
<!-- То же, что в drawable/: тёмный фон и молния по центру. Отдельный файл
     остаётся, чтобы не зависеть от системной темы устройства — приложение
     всегда тёмное, независимо от того, что выбрано в настройках Android. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/launch_background" />
    <item>
        <bitmap
            android:gravity="center"
            android:src="@mipmap/ic_launcher_foreground" />
    </item>
</layer-list>
