"""Убрать внутридневные бары MOEX, загруженные со сдвигом на три часа.

Адаптер помечал московское время как UTC (``replace(tzinfo=UTC)``), и весь
внутридневной ряд срочного рынка уезжал на три часа вперёд. На живом сервере
это выглядело как бар из будущего: свежайший бар был датирован 19:00 UTC,
когда серверные часы показывали 16:29 UTC.

Одной правки кода мало. Бар опознаётся тройкой инструмент-таймфрейм-время,
поэтому исправленная загрузка **добавила бы** правильные бары рядом с
неправильными, а не заменила их: в одном ряду оказались бы две копии каждой
свечи со сдвигом в три часа. Детекторы структуры на таком ряду находят
несуществующие импульсы.

Сдвинутые бары удаляются, а не сдвигаются обратно: биржа отдаёт их заново
на первом же проходе загрузки, и перевыгрузка из источника надёжнее
арифметики по испорченным данным.

Дневные бары не затронуты. У них начало — московская полночь, а торговый
день и раньше сохранялся как дата, поэтому их время не менялось.
"""

from __future__ import annotations

from alembic import op

revision = "0003_drop_shifted_moex_bars"
down_revision = "0002_append_only"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.execute(
        """
        DELETE FROM bars
        WHERE source = 'moex'
          AND timeframe <> '1d'
        """
    )


def downgrade() -> None:
    # Восстановить удалённые бары нечем и незачем: они были неверны. Загрузка
    # заполнит ряд из источника.
    pass
